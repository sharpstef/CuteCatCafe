     <section class="navbar main-nav navbar-fixed-top" role="navigation">
          <div class="container">

               <div class="navbar-header">
                    <button class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                         <span class="icon icon-bar"></span>
                         <span class="icon icon-bar"></span>
                         <span class="icon icon-bar"></span>
                    </button>
                    <a href="/" class="navbar-brand">Cute Cat Cafe</a>
               </div>
               <div class="collapse navbar-collapse">
                    <ul class="nav navbar-nav nav-first">
                        {{#if menu_data}}
						{{#each menu_data}}
							<li><a href="{{this.REF}}">{{this.NAME}}</a></li>
						{{/each}}
					{{/if}}
                    </ul>

                    <ul class="nav navbar-nav navbar-right">
                         <li><a href="./checkout"><i class="fa fa-shopping-cart"></i></a></li>
                         <li><a href="./account"><i class="fa fa-user"></i></a></li>
                         {{#if login_status}}
                              <a href="{{login_status.REF}}" id="user" class="button">{{login_status.NAME}}</a>
                         {{/if}}
                    </ul>
               </div>

          </div>
     </section>